spring:
    cloud:
        consul:
            config:
                fail-fast: true
                enabled: true
                prefix: config
                defaultContext: demo-service
                data-key: application-yml
                profileSeparator: ':::'
                format: yaml
