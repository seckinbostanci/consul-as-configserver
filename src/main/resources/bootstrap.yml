spring:
    application:
        name: demo
    cloud:
        consul:
            config:
                fail-fast: true
                enabled: true
                prefix: config
                defaultContext: application
                data-key: data
                profileSeparator: '::'
                format: yaml
            host: localhost
            port: 8500
